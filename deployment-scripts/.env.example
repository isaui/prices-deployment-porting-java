# PRICES Deployment Scripts - Environment Variables
# Copy to .env and source before running: source .env

# Parent domain untuk auto-generate URLs
PRICES_DOMAIN=skripsi.isacitra.com

# Directory untuk extract artifacts
PRICES_DEPLOYMENTS_DIR=/var/prices/deployments

# Nginx config directory (host path, mounted ke container)
PRICES_NGINX_CONFIG_DIR=/var/prices/nginx/conf.d

# Nginx container name
PRICES_NGINX_CONTAINER=prices-nginx
