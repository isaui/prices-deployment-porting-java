# Server
PORT=8080

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=prices
DB_PASSWORD=prices123
DB_NAME=prices_deployment

# JWT (minimum 32 characters for HS256)
JWT_SECRET=your-secret-key-here-minimum-32-characters

# Domain (parent domain for project URLs, e.g., skripsi.isacitra.com -> project.skripsi.isacitra.com)
PRICES_DOMAIN=skripsi.isacitra.com

# Protocol (http or https)
PRICES_PROTOCOL=https

# External PostgreSQL for deployed projects
PRICES_EXTERNAL_DB_HOST=10.119.106.82
PRICES_EXTERNAL_DB_PORT=5432
PRICES_EXTERNAL_DB_USER=deployment
PRICES_EXTERNAL_DB_PASSWORD=deployment123

# Admin user (created on startup if not exists)
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@prices.local
ADMIN_PASSWORD=admin123

# Internal API Key (for deploy/ssh endpoints)
INTERNAL_API_KEY=your-internal-api-key-here
