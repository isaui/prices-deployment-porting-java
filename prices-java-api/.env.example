# Server
PORT=8080

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=prices
DB_PASSWORD=prices123
DB_NAME=prices_deployment

# JWT (minimum 32 characters for HS256)
JWT_SECRET=your-secret-key-here-minimum-32-characters

# Domain (parent domain for project URLs, e.g., prices.dev -> project.prices.dev)
PRICES_DOMAIN=prices.dev
